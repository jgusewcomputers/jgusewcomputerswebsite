# Cloudflare redirect rules
# Docs: https://developers.cloudflare.com/workers/static-assets/redirects/
#
# NOTE: The www -> apex redirect is handled in _worker.js, because Workers
# static assets _redirects rules only allow relative URLs (error 100324).
# www.jgusewcomputers.com must still be configured as a custom domain on
# the Worker for that redirect to fire.
